//A for loop has 3 parts:

// a starting point: var i = 0
// a stop/end point i ≤ 100
// a “step” i++ (a short version of i = i+1)

// for(declare variable; input operation; increment by 1) {
    // log variable to the console
// }

//Then invoke the created function


// create a function that prints number 1-10


function printNumbers() {
    for(var i=1; i <= 10; i++) {
        console.log(i);
    }
}

printNumbers(); //invoke function